doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title=title

    body
        h1=title
        each line in lines
            p=line
        img(src=image, alt="Image")
        br
        br
        button(onclick="window.location.href = '../" + other + "'") To Other Page